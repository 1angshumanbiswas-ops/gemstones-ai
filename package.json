{
  "name": "gemstones-ai",
  "version": "0.3.0-phase3",
  "private": true,
  "description": "Gemstones_AI - MCP orchestrator architecture, Phase 3: deterministic core + rule-graph + gemology/certificate/consumer-protection layers",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/ephemeris-mcp --workspace=packages/geo-timezone-mcp --workspace=packages/numerology --workspace=packages/dasha --workspace=packages/rule-graph --workspace=packages/gemology && npm run build --workspace=packages/orchestrator",
    "test": "npm run test --workspace=packages/ephemeris-mcp --workspace=packages/geo-timezone-mcp --workspace=packages/numerology --workspace=packages/dasha --workspace=packages/rule-graph --workspace=packages/gemology --workspace=packages/orchestrator",
    "dev:orchestrator": "npm run dev --workspace=packages/orchestrator"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
